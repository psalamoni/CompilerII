program ex2

/* declaracao de variaveis */
var a, b, c, d, n: integer;

/* declaracao de procedimentos */
procedure p(x, y: integer)
  var k: integer
begin
  x := k
end

/*  corpo * principal / */

begin
    d:=2;
    a:=1;
    c:=4;
    write(a,d);
    if d > 1
    then b := a
    else n := 1 
    $;
    write (c)
end.
